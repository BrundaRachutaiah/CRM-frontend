import DashboardLayout from '../layout/DashboardLayout';
import Card from '../components/Card';

export default function Settings() {
  return (
    <DashboardLayout>
      <h2>Settings</h2>
      <Card>
        <p>Workspace settings page is ready for future preferences and account controls.</p>
      </Card>
    </DashboardLayout>
  );
}
